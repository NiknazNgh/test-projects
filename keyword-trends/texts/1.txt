🧭 Project #13: Multi-File Keyword Trend Analyzer (AI-Lite NLP Project)

Goal:
Analyze multiple .txt reports (e.g. monthly summaries, feedback logs, notes), and automatically find:

Top keywords per file

Which keywords appear most consistently across all files

Trend of word usage over time (if filenames contain months or dates)

You’ll end up with a data-driven Excel dashboard showing how themes evolve over time — pure local NLP, no internet needed.

⚙️ Setup

Make a folder:

C:\Users\NEGAHDN\Downloads\test projects\keyword-trends


Install dependencies:

pip install pandas nltk openpyxl

🧾 Code (keyword_trend_analyzer.py)
import os
import re
import pandas as pd
from nltk.corpus import stopwords
from nltk.tokenize import word_tokenize
from collections import Counter
import nltk

# Setup NLTK
nltk.download("punkt", quiet=True)
nltk.download("stopwords", quiet=True)

FOLDER = r"C:\Users\NEGAHDN\Downloads\test projects\keyword-trends\texts"

def clean_words(text):
    stop_words = set(stopwords.words("english"))
    tokens = [w.lower() for w in word_tokenize(text) if w.isalnum()]
    return [w for w in tokens if w not in stop_words]

def extract_month(filename):
    months = [
        "jan","feb","mar","apr","may","jun",
        "jul","aug","sep","oct","nov","dec"
    ]
    for m in months:
        if m in filename.lower():
            return m.capitalize()
    return "Unknown"

def analyze_folder(folder):
    results = []
    total_counts = Counter()

    for file in os.listdir(folder):
        if file.endswith(".txt"):
            path = os.path.join(folder, file)
            with open(path, "r", encoding="utf-8", errors="ignore") as f:
                text = f.read()

            words = clean_words(text)
            counts = Counter(words)
            top10 = counts.most_common(10)
            month = extract_month(file)

            for word, freq in top10:
                results.append({
                    "File": file,
                    "Month": month,
                    "Keyword": word,
                    "Frequency": freq
                })

            total_counts.update(counts)
            print(f"📄 Processed {file}")

    df = pd.DataFrame(results)
    summary = pd.DataFrame(total_counts.most_common(20), columns=["Keyword", "Total_Frequency"])

    with pd.ExcelWriter(os.path.join(folder, "Keyword_Trend_Report.xlsx")) as writer:
        df.to_excel(writer, index=False, sheet_name="Per File Top Keywords")
        summary.to_excel(writer, index=False, sheet_name="Overall Top Keywords")

    print("✅ Keyword trend report saved successfully!")

if __name__ == "__main__":
    analyze_folder(FOLDER)

📈 Output example (Excel)
Sheet 1 — Per File Top Keywords
File	Month	Keyword	Frequency
report_jan.txt	Jan	water	23
report_feb.txt	Feb	energy	19
report_feb.txt	Feb	pump	17
report_mar.txt	Mar	energy	21
Sheet 2 — Overall Top Keywords
Keyword	Total_Frequency
energy	57
pump	43
water	39
flow	28
🌟 Why this project is awesome

Tracks recurring themes in your documents.

Recognizes month names from filenames automatically.

Great for content analytics, reports, or progress summaries.

Fully offline and customizable — change stopwords or keywords easily.